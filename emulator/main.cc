#include "ui/ui.hh"
#include "config/config.hh"

#include <curses.h>

int main(int argc, char* argv[])
{
    Config config(argc, argv);
    /*
    initscr();
    resize_term(40, 130);
    noecho();
    keypad(stdscr, true);
    refresh();

    UI ui;

    while (ui.active())
        ui.execute();
    */
}